/* import { YamamahSmsService } from '@app/services/sms/yamamah-sms.service';
import config from '@app/config/config';

export class SmsServiceFactory {
  constructor() {
    let smsService;
    switch (config.SMS.service_name) {
      case 'yamamah':
        smsService = new YamamahSmsService();
        break;
    }

    return smsService;
  }
}
 */
